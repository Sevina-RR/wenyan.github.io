# 修改主页内容

1. 改姓名、学校、邮箱、研究兴趣：
   打开 `src/data/profile.ts`
2. 改 About 介绍：
   打开 `src/app/page.tsx`
3. 改论文和项目：
   继续编辑 `src/data/profile.ts`
4. 换头像：
   把你的图片放到 `public/`，然后改 `src/components/ProfileSidebar.tsx`
5. 改粉色配色：
   打开 `src/app/globals.css`
