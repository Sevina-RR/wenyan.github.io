# GitHub Pages 发布步骤

1. 新建仓库，仓库名建议使用 `你的GitHub用户名.github.io`。
2. 把本文件夹里的所有文件和文件夹上传到仓库根目录。
3. 进入仓库 `Settings`。
4. 打开左侧 `Pages`。
5. 在 `Build and deployment` 里把 `Source` 设为 `GitHub Actions`。
6. 回到仓库上方 `Actions`，等待 `Deploy homepage to GitHub Pages` 运行完成。
7. 打开你的主页地址。

不要只上传 zip，也不要把整个文件夹再套一层上传。
